#include <iostream>

// The euclidean algorithm is an efficient method for computing the greatest common divisor (gdc) of two integers 
// ?? Personal note: seems to be another way of doing a binary search? Or at the very least using two pointer. 

double gdc(int x, int y)
{ 
    return 0.0;
}

int main()
{
    std::cout << "Hello, world!"; 
}